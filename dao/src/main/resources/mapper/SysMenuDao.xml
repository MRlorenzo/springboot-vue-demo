<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one or more
  ~ contributor license agreements.  See the NOTICE file distributed with
  ~ this work for additional information regarding copyright ownership.
  ~ The ASF licenses this file to You under the Apache License, Version 2.0
  ~ (the "License"); you may not use this file except in compliance with
  ~ the License.  You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.spmul.dao.shiro.SysMenuDao">
	<cache-ref namespace="org.spmul.dao.shiro.SysMenuDao"/>
	<cache readOnly="true"/>

	<select id="queryObject" resultType="org.spmul.entity.shiro.SysMenuEntity">
		select * from sys_menu where menu_id = #{value}
	</select>

	<insert id="save" parameterType="org.spmul.entity.shiro.SysMenuEntity">
		insert into sys_menu
		(
			`menu_id`,
			`parent_id`,
			`name`,
			`en_name`,
			`url`,
			`perms`,
			`type`,
			`icon`,
			`order_num`
		)
		values
		(
			#{menuId},
			#{parentId},
			#{name},
			#{enName},
			#{url},
			#{perms},
			#{type},
			#{icon},
			#{orderNum}
		)
	</insert>

	<select id="queryListParentId" resultType="org.spmul.entity.shiro.SysMenuEntity">
		select * from sys_menu where parent_id = #{parentId} order by order_num asc
	</select>

	<select id="queryNotButtonList" resultType="org.spmul.entity.shiro.SysMenuEntity">
		select * from sys_menu where type != 2 order by order_num asc
	</select>

	<select id="queryList" resultType="org.spmul.entity.shiro.SysMenuEntity">
		select m.*,(select p.name from sys_menu p where p.menu_id = m.parent_id) as parentName
			from sys_menu m
		<choose>
			<when test="sidx != null and sidx.trim() != ''">
				order by m.${sidx} ${order}
			</when>
			<otherwise>
				order by m.order_num asc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<!-- 查询用户的权限列表 -->
	<select id="queryUserList" resultType="org.spmul.entity.shiro.SysMenuEntity">
		select distinct m.*,(select p.name from sys_menu p where p.menu_id = m.parent_id) as parentName
			from sys_user_role ur
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
			LEFT JOIN sys_menu m on rm.menu_id = m.menu_id
		where ur.user_id = #{userId} order by m.order_num asc
	</select>

	<select id="queryTotal" resultType="int">
		select count(*) from sys_menu
	</select>

	<update id="update" parameterType="org.spmul.entity.shiro.SysMenuEntity">
		update sys_menu
		<set>
			<if test="parentId != null">`parent_id` = #{parentId}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="enName != null">`en_name` = #{enName}, </if>
			<if test="url != null">`url` = #{url}, </if>
			<if test="perms != null">`perms` = #{perms}, </if>
			<if test="type != null">`type` = #{type}, </if>
			<if test="icon != null">`icon` = #{icon}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}</if>
		</set>
		where menu_id = #{menuId}
	</update>

	<delete id="deleteBatch">
		delete from sys_menu where menu_id in
		<foreach item="menuId" collection="array" open="(" separator="," close=")">
			#{menuId}
		</foreach>
		;
		delete from sys_role_menu where menu_id in
		<foreach item="menuId" collection="array" open="(" separator="," close=")">
			#{menuId}
		</foreach>
	</delete>


	<resultMap id="allInfo" type="org.spmul.entity.shiro.SysMenuEntity">
		<collection property="list" column="menu_id" select="org.spmul.dao.shiro.SysMenuDao.queryListParentId"/>
	</resultMap>

	<select id="queryUserListAllMenu" resultType="org.spmul.entity.shiro.SysMenuEntity">
		SELECT * FROM
		sys_menu m
		WHERE EXISTS (
			select 1 from sys_user_role ur
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
			where ur.user_id = #{userId} and  m.menu_id = rm.menu_id
		)
		ORDER BY type,order_num ASC
	</select>

	<select id="queryListAllMenu" resultType="org.spmul.entity.shiro.SysMenuEntity">
		SELECT * FROM
		sys_menu
		ORDER BY type,order_num ASC
	</select>
</mapper>
